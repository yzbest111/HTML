HTML语义化的概念：
	在编程中，语义指的是一段代码的含义。在HTML中，则表示某个元素有什么作用，扮演了什么角色。HTML语义化元素，可以清楚的向浏览器和开发者描述其意义。例如form、table、img等。
	很多时候，我们书写HTML，为了方便都会直接用div和span标签，再通过class来确定具体样式。网站哪一部分为标题，哪一部分为导航，哪一部分为头部和底部，都只能通过class来确定。
	但class命名规范却又没有一套统一的标准，因此导致很多时候无法确定整体网站的结构。
	在html5出现后，添加了许多语义化标签，例如header、footer、main、nav、aside、article、section等。有了这些语义化标签，我们在开发中可以根据不同的内容使用合适的标签进行开发。
	
语义化的优点：
	1.便于开发者阅读和理解代码，通过不同标签明白每个模块的作用；
	2.结构更加清晰，在样式没有加载前也有较为明确的结构。例如img标签，在图片无法加载的情况下，有alt属性，便可提示用户此图片代表的内容；
	3.利于SEO，语义化便于搜索引擎爬虫理解，和搜索引擎建立良好的沟通，能让爬虫爬取更多关键有效的信息；
	4.方便其他设备阅读，如屏幕阅读器、盲人设备等。
